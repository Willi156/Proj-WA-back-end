# Use DATABASE_URL environment variable if present; fallback to local Postgres
spring.datasource.url=${DATABASE_URL:postgresql://postgres:cvyTtsvHxrfONVSjNDnxEScycSGICSZK@postgres.railway.internal:5432/railway}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASS:cvyTtsvHxrfONVSjNDnxEScycSGICSZK}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate/JPA not used, keep datasource initialization simple
spring.main.allow-bean-definition-overriding=true

# Server port (optional)
server.port=${PORT:3000}

# Email configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
